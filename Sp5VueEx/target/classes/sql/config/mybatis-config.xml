<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<settings>
		<!-- <setting name="cacheEnabled" value="false"/> --> 		<!-- 설정에서 각 매퍼에 설정된 캐시를 전역적으로 사용할지 말지에 대한 여부 (기본값 true) -->
		<!-- <setting name="lazyLoadingEnabled" value="true"/> -->	<!-- 늦은 로딩을 사용할지에 대한 여부. 사용하지 않는다면 모두 즉시 로딩할 것이다. (기본값 false) -->
		<!-- <setting name="mapUnderscoreToCamelCase" 	value="true" /> -->	<!-- 전통적인 데이터베이스 칼럼명 형태인 A_COLUMN을 camelCase형태의 자바 프로퍼티명 형태인 aColumn으로 자동으로 매핑하도록 함 -->
		<setting name="callSettersOnNulls" 			value="true"/>	<!-- 가져온 값이 null일때 setter나 맵의 put 메소드를 호출할지를 명시 -->
	</settings>

	<!-- Value Object(DTO) typeAliases 설정 -->
	<!-- 타입 별칭은 자바 타입에 대한 좀더 짧은 이름이다. 오직 XML 설정에서만 사용되며, 타이핑을 줄이기 위해 존재한다. -->
	<typeAliases>
		<typeAlias alias="bbsDto" 				type="com.sp.app.bbs.dto.BbsDto" />
		<typeAlias alias="bbsFileContentsDto" 	type="com.sp.app.bbs.dto.BbsFileContentsDto" />
	</typeAliases>

</configuration>